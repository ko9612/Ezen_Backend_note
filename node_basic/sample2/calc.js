const calc = {
  plus: function (a, b) {
    // 정사각형 면적
    return a + b;
  },
  minus: function (a, b) {
    // 원의 면적
    return a - b;
  },
  mul: function (a, b) {
    // 직감삭형 면적
    return a * b;
  },
  div: function (a, b) {
    // 직감삭형 면적
    return a / b;
  },
};

module.exports = calc;
